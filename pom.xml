<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>is.apim.selfsignup.handler</groupId>
    <artifactId>is-apim-selfsignup-handler</artifactId>
    <version>${build-version}</version>
    <packaging>bundle</packaging>
    <name>is-apim-selfsignup-handler</name>

    <properties>
        <java.version>1.8</java.version>
        <build-version>1.0-SNAPSHOT</build-version>
        <org.wso2.carbon.identity.recovery.version>1.11.48</org.wso2.carbon.identity.recovery.version>
        <org.wso2.carbon.identity.event.handler.notification.version>1.9.43</org.wso2.carbon.identity.event.handler.notification.version>
        <org.wso2.carbon.user.core.version>4.10.42</org.wso2.carbon.user.core.version>
        <org.osgi.service.component.annotations.version>1.4.0</org.osgi.service.component.annotations.version>
        <org.osgi.framework.version>1.9.0</org.osgi.framework.version>
        <maven-bundle-plugin-version>3.2.0</maven-bundle-plugin-version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.osgi</groupId>
            <artifactId>org.osgi.service.component.annotations</artifactId>
            <version>${org.osgi.service.component.annotations.version}</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.osgi</groupId>
            <artifactId>org.osgi.framework</artifactId>
            <version>${org.osgi.framework.version}</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.wso2.carbon</groupId>
            <artifactId>org.wso2.carbon.user.core</artifactId>
            <version>${org.wso2.carbon.user.core.version}</version>
        </dependency>
        <dependency>
            <groupId>org.wso2.carbon.identity.event.handler.notification</groupId>
            <artifactId>org.wso2.carbon.identity.event.handler.notification</artifactId>
            <version>${org.wso2.carbon.identity.event.handler.notification.version}</version>
        </dependency>
        <dependency>
            <groupId>org.wso2.carbon.identity.governance</groupId>
            <artifactId>org.wso2.carbon.identity.recovery</artifactId>
            <version>${org.wso2.carbon.identity.recovery.version}</version>
        </dependency>
    </dependencies>

    <repositories>
        <repository>
            <id>wso2-nexus</id>
            <name>WSO2 internal Repository</name>
            <url>https://maven.wso2.org/nexus/content/groups/wso2-public/</url>
            <releases>
                <enabled>true</enabled>
                <updatePolicy>daily</updatePolicy>
                <checksumPolicy>ignore</checksumPolicy>
            </releases>
        </repository>
    </repositories>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.felix</groupId>
                <artifactId>maven-bundle-plugin</artifactId>
                <version>${maven-bundle-plugin-version}</version>
                <extensions>true</extensions>
                <configuration>
                    <instructions>
                        <Bundle-SymbolicName>${project.artifactId}</Bundle-SymbolicName>
                        <Bundle-Name>${project.artifactId}</Bundle-Name>
                        <Export-Package>
                            is.apim.selfsignup.handler.*,
                        </Export-Package>
                        <Import-Package>
                            org.wso2.carbon.user.core.*,
                            org.wso2.carbon.identity.event.handler.notification.*,
                            org.wso2.carbon.identity.recovery.*,
                            org.osgi.service.component.annotations.*,
                            org.osgi.framework.*
                        </Import-Package>
                        <DynamicImport-Package>*</DynamicImport-Package>
                    </instructions>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <source>${java.version}</source>
                    <target>${java.version}</target>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>